services:
  app:
    container_name: portfolio-dev
    build:
      context: .
      target: development  # This tells Docker to stop at the 'development' stage
    ports:
      - "5301:5301"
    networks:
      - pangolin

networks:
  pangolin:
    external: true
